import {
    main,
    container,
    containerFluid
} from "./neomat/core.js";

const footerStyles = {
    main,
    container,
    containerFluid,
    footer: {
        backgroundColor: "#222",
        color: "#FFF"
    }
};

export default footerStyles;
