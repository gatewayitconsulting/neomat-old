import {
    container,
    containerFluid
} from "./neomat/core.js";

const homePageStyles = {
    main: {
        backgroundColor: "#222",
        color: "#FFF"
    },
    container,
    containerFluid
};

export default homePageStyles;
